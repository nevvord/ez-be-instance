// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider        = "prisma-client-js"
  output          = "../node_modules/.prisma/client"
  previewFeatures = ["prismaSchemaFolder"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Import schema fragments
// We use the /// syntax to import models from separate files
/// import user from "models/user.prisma"
/// import session from "models/session.prisma"
/// import event from "models/event.prisma"
/// import refresh-token from "models/refresh-token.prisma" 